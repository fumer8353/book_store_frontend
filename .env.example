# Local development only — copy to .env
VITE_API_URL=http://localhost:5000/api

# Azure Static Web Apps — do not commit production values here.
# Set the same variable in Azure portal:
# Static Web App → Configuration → Environment variables
# Name:  VITE_API_URL
# Value: https://your-backend.azurewebsites.net/api
